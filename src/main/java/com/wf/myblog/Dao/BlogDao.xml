<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wf.myblog.Dao.BlogDao">

    <select id="getAllBlog" resultType="com.wf.myblog.Bean.Blog">
        select b.id as id, t.name as type, b.recommend as recommend, b.update_time as updateTime
        from blog.blog as b, blog.type as t
        where b.type_id=t.id
    </select>

</mapper>